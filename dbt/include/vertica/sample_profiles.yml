default:
  outputs:

    dev:
      type: vertica
      threads: [1 or more]
      host: [host]
      port: [port]
      username: [dev_username]
      password: [dev_password]
      database: [database]
      schema: [dev_schema]
      retries: [1 or more]
      backup_server_node: [backup_server_node]
      connection_load_balance: True

    prod:
      type: vertica
      threads: [1 or more]
      host: [host]
      port: [port]
      user: [prod_username]
      pass: [prod_password]
      dbname: [dbname]
      schema: [prod_schema]

  target: dev